#Database
spring.h2.console.enabled=${h2.console.enabled}
spring.h2.console.path=${h2.console.path}
spring.datasource.url=${h2.datasource.url}

# Vault
spring.config.import=optional:vault://
spring.cloud.config.enabled=false
spring.cloud.vault.enabled=true
spring.cloud.vault.uri=http://localhost:8200
spring.cloud.vault.authentication=token
spring.cloud.vault.token=hvs.nVrRBDVjqsvyFL55LcIBgiyb

spring.cloud.vault.kv.enabled=true
spring.cloud.vault.kv.default-context=product
spring.cloud.vault.kv.backend=nextShop

# Minio
spring.minio.bucket.name=${minio.bucket.name}
spring.minio.access-key=${minio.access-key}
spring.minio.secret-key=${minio.secret-key}
spring.minio.url=${minio.url}
